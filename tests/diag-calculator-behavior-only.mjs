import assert from "node:assert/strict";import {runAdapter} from "../src/adapters-extra49.js";const p="medical_clinical_calculators";const bmi=await runAdapter(p,"bmi",{weight_kg:70,height_m:1.75});assert.equal(bmi.bmi,22.9);assert.equal(bmi.adult_who_category,"normal-range");const e=await runAdapter(p,"egfr_2021_creatinine",{age_years:50,sex:"male",serum_creatinine:1,creatinine_unit:"mg/dL"});assert.equal(e.egfr_ml_min_1_73m2,91.7);const c=await runAdapter(p,"egfr_2021_creatinine_cystatin_c",{age_years:50,sex:"male",serum_creatinine:1,cystatin_c_mg_l:1});assert.equal(c.egfr_ml_min_1_73m2,88.1);const crb=await runAdapter(p,"crb65",{confusion:false,respiratory_rate:32,systolic_bp:110,diastolic_bp:70,age_years:70});assert.equal(crb.score,2);const curb=await runAdapter(p,"curb65",{confusion:true,urea_mmol_l:8,respiratory_rate:31,systolic_bp:88,diastolic_bp:62,age_years:70});assert.equal(curb.score,5);const q=await runAdapter(p,"qsofa",{respiratory_rate:22,systolic_bp:100,altered_mentation:false});assert.equal(q.score,2);console.log(JSON.stringify({ok:true,suite:"calculator-behavior-only"}));