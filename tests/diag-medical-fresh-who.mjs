import assert from "node:assert/strict";const BASE="https://intelligence-worker.a15280020511.workers.dev";const r=await fetch(`${BASE}/v1/provider/who_icd11/readiness`,{headers:{accept:"application/json"}});const b=await r.json().catch(()=>null);assert.equal(r.status,200,`WHO_READINESS:${r.status}:${JSON.stringify(b)}`);assert.equal(b?.ok,true);assert.equal(b?.configured,false,`EXPECTED_UNCONFIGURED:${JSON.stringify(b)}`);console.log(JSON.stringify({ok:true,suite:"who-icd11-unconfigured-classification",configured:false,secrets_redacted:true}));