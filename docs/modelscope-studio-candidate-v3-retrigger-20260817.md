# ModelScope Studio candidate v3 diagnostic retrigger

No diagnostic or runtime behavior change. This marker only forces a fresh Cloudflare Workers Build after the first candidate-v3 check-run remained in progress beyond its own request timeout.
